# Configuration file
# Classification (U)
# Unclassified until filled.

host = ["HOST_NAME1", "HOST_NAME2"]
port = 9200

# Name of the test repository directory path
# Must be a shared mount between all Elasticsearch databases in the cluster.
base_repo_dir = "BASE_REPO_DIRECTORY/"
